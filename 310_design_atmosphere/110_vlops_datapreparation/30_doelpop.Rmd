# Puntenlaag doelpopulatie aanmaken voor analyse

We hanteren het GRTS master grid:

```{r}
vlops_pred_points <- 
    read_GRTSmh(datapath) %>% 
    rasterToPoints(spatial = TRUE) %>% 
    st_as_sf %>% 
    st_join(polyg_sel)
vlops_pred_points
```

