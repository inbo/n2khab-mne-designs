```{r interaction-type-{{dfname_hyphen}}, fig.cap = 'Het verloop in de tijd van het aantal bemeten locaties per jaar, per type van de doelpopulatie in meetnet {{dfname2}}. De waarden zijn gestandaardiseerd per type, waarbij het jaar met het hoogste aantal locaties de waarde 1 krijgt (0 = wit = geen locaties). Alle types uit de doelpopulatie zijn weergegeven, desgevallend ook types zonder data in de analysedataset. Voor het totaal aantal observaties per type, zie Figuur \\@ref(fig:barplot-{{dfname_hyphen}}-type).', out.width='70%', fig.height=(2 + 6/60 * length(type_levels)), fig.width=6, warning=FALSE}

print(p)
```
