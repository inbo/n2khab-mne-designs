```{r}
dataset <- gw33
respvars <- c("no3", "nh4", "po4")
stratumvar <- "ecoregion" # to support heterogeneity in residuals
ts_stratumvar <- "ecoregion" # for interaction with space and time
```

```{r results="asis", warning=FALSE, message=FALSE}
walk(respvars, function(respvar) {
  knit_expand("_941_gw33.Rmd") %>% 
          {knit_child(text = ., 
                      quiet = TRUE,
                      envir =  environment())} %>% 
          cat(sep = '\n')
})
```

```{r eval=FALSE, echo=FALSE}
# for manual testing one value of respvars
respvar <- respvars[3]
```

