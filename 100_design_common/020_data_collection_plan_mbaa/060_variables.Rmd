## Milieuvariabelen en meetvariabelen {#meetvariabelen}

Voor de meetnetten in het MBAG werden primaire milieuvariabelen geselecteerd om de primaire standplaatsfactoren te vertegenwoordigen.
Daarnaast dienen verklarende aanvullende milieuvariabelen en omgevingsvariabelen om de primaire milieuvariabelen  te interpreteren. 
Ten slotte werden variabelen voor kwaliteitscontrole gekozen om de milieuvariabelen te controleren.

Voor de praktische implementatie op het terrein en in het labo werden de metingen van de milieuvariabelen gespecifieerd in de vorm van meetvariabelen.

De primaire milieuvariabelen, de verklarende milieuvariabelen en de milieuvariabelen voor kwaliteitscontrole zijn opgelijst in onderstaande Tabel \@ref(tab:milieuvar-gw-03-3) voor `GW_03.3`, Tabel \@ref(tab:milieuvar-gw-05-1-aq) voor `GW_05.1_aq`, Tabel \@ref(tab:milieuvar-gw-05-1-quarries) voor `GW_05.1_quarries`,  \@ref(tab:milieuvar-gw-05-1-terr) voor `GW_05.1_terr`, Tabel \@ref(tab:milieuvar-soil-03-2) voor `SOIL_03.2`, Tabel \@ref(tab:milieuvar-surf-03-4-lentic) voor `SURF_03.4_lentic` en Tabel \@ref(tab:milieuvar-surf-03-4-lotic) voor `SURF_03.4_lotic`.
Het opmeten van de meetvariabelen vereist een inzet op terrein en, in het geval van de eutrofiëringsmeetnetten, ook in het labo voor chemische analyse.
Niet elke meetvariabele vraagt een unieke analyse- of veldmethode: vaak kunnen meerdere meetvariabelen met een zelfde methode of een zelfde voorbereiding worden bepaald of worden ze steeds samen uitgevoerd.
Omwille van organisatorische redenen worden meetvariabelen gegroepeerd in **variabelensets** en volgens **veldmethode**.
Labo-analyses die steeds op een specifiek type staal worden uitgevoerd vormen het standaardpakket voor dat type staal.
Extra analyses die daarbuiten vallen, zijn apart vermeld.
Meetvariabelen die een zelfde handeling in het veld vereisen of steeds samen worden uitgevoerd, zijn gegroepeerd in veldmethodes, die rechtstreeks gelinkt kunnen worden aan veldprotocollen.

De voorliggende verklarende milieuvariabelen en de milieuvariabelen voor kwaliteitscontrole worden bij voorkeur allemaal opgemeten of via secundaire bronnen verworven.
Het is echter mogelijk dat financiële kaders deze lijsten inperken.
In dat geval dient een bijkomende selectie te worden uitgevoerd op basis van een kosten-batenanalyse.

```{r read-gsheet-envvars, eval = !file.exists(file.path(datapath, "text/raw/vars_phase_1_mbag.yml")) || params$update_raw_textdata, message = FALSE, warning = FALSE}
gs_id_envvars <- "1gJb2nY-Cs-SCNMpz0sGFslb0j69mzNx5BJu9tQXpHfc"
read_sheet(ss = gs_id_envvars, sheet = "vars_phase_1_mbag") %>%
  write_vc(
    file = "vars_phase_1_mbag",
    root = file.path(datapath, "text/raw"),
    optimize = FALSE,
    strict = FALSE
  )
```

```{r read-vcfile-envvars}
mv <-
  read_vc(
    file = "vars_phase_1_mbag",
    root = file.path(datapath, "text/raw")
  ) %>%
  as_tibble() %>%
  select(
    Meetnet = meetnet,
    Standplaatsfactor = standplaatsfactor,
    Milieuvariabele = milieuvar,
    `Afkorting milieuvariabele` = afk_milieuvar,
    Meetvariabele = meetvar,
    Primair = primair,
    Verklarend = verklarend,
    `Kwaliteits-controle` = kwaliteitscontrole,
    Variabelenset = variable_set,
    Veldmethode = field_method
  )
```

```{r envvar-table-function}
print_envvar_table <- function(scheme) {
  sch <- str_replace_all(scheme, "_|\\.", "-") %>% str_to_lower()
  mv %>%
    filter(Meetnet == scheme) %>%
    select(Standplaatsfactor:Veldmethode) %>%
    kbl_bt(
      align = "llllcccll",
      caption = str_glue("(ref:milieuvar-{sch})"),
      longtable = TRUE
    ) %>%
    our_column_spec(
      column = c(2:9),
      width = str_c(c(4, 2, 4, 1.5, 1.5, 1.5, 2.5, 2.5), "cm")
    ) %>%
    longtable_styling() %>%
    landscape()
}
```


(ref:milieuvar-gw-03-3) De primaire milieuvariabelen die de primaire standplaatsfactoren vertegenwoordigen, de verklarende milieuvariabelen die de vermelde primaire milieuvariabele(n) verklaren en de milieuvariabelen voor kwaliteitscontrole voor het meetnet eutrofiëring via het grondwater (`GW_03.3`) (nvt: niet van toepassing).

```{r milieuvar-gw-03-3}
print_envvar_table("GW_03.3")
```

(ref:milieuvar-gw-05-1-aq) De primaire milieuvariabelen die de primaire standplaatsfactoren vertegenwoordigen, de verklarende milieuvariabelen die de vermelde primaire milieuvariabele(n) verklaren en de milieuvariabelen voor kwaliteitscontrole voor het meetnet verdroging via het grondwater - deelmeetnet aquatische types (`GW_05.1_aq`) (nvt: niet van toepassing).

```{r milieuvar-gw-05-1-aq}
print_envvar_table("GW_05.1_aq")
```


(ref:milieuvar-gw-05-1-quarries) De primaire milieuvariabelen die de primaire standplaatsfactoren vertegenwoordigen, de verklarende milieuvariabelen die de vermelde primaire milieuvariabele(n) verklaren en de milieuvariabelen voor kwaliteitscontrole voor het meetnet verdroging via het grondwater - deelmeetnet mergelgroeven (`GW_05.1_quarries`) (nvt: niet van toepassing).

```{r milieuvar-gw-05-1-quarries}
print_envvar_table("GW_05.1_quarries")
```


(ref:milieuvar-gw-05-1-terr) De primaire milieuvariabelen die de primaire standplaatsfactoren vertegenwoordigen, de verklarende milieuvariabelen die de vermelde primaire milieuvariabele(n) verklaren en de milieuvariabelen voor kwaliteitscontrole voor het meetnet verdroging via het grondwater - deelmeetnet terrestrische types (`GW_05.1_terr`) (nvt: niet van toepassing).

```{r milieuvar-gw-05-1-terr}
print_envvar_table("GW_05.1_terr")
```


(ref:milieuvar-soil-03-2) De primaire milieuvariabelen die de primaire standplaatsfactoren vertegenwoordigen, de verklarende milieuvariabelen die de vermelde primaire milieuvariabele(n) verklaren en de milieuvariabelen voor kwaliteitscontrole voor het meetnet eutrofiëring via de bodem (`SOIL_03.2`) (nvt: niet van toepassing).

```{r milieuvar-soil-03-2}
print_envvar_table("SOIL_03.2")
```

(ref:milieuvar-surf-03-4-lentic) De primaire milieuvariabelen die de primaire standplaatsfactoren vertegenwoordigen, de verklarende milieuvariabelen die de vermelde primaire milieuvariabele(n) verklaren en de milieuvariabelen voor kwaliteitscontrole voor het meetnet eutrofiëring via het oppervlaktewater - deelmeetnet lentische types (`SURF_03.4_lentic`).

```{r milieuvar-surf-03-4-lentic}
print_envvar_table("SURF_03.4_lentic")
```

(ref:milieuvar-surf-03-4-lotic) De primaire milieuvariabelen die de primaire standplaatsfactoren vertegenwoordigen, de verklarende milieuvariabelen die de vermelde primaire milieuvariabele(n) verklaren en de milieuvariabelen voor kwaliteitscontrole voor het meetnet eutrofiëring via het oppervlaktewater - deelmeetnet lotische types (`SURF_03.4_lotic`).

```{r milieuvar-surf-03-4-lotic}
print_envvar_table("SURF_03.4_lotic")
```
